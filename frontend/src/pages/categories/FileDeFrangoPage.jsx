import CategoryPage from '../CategoryPage.jsx';

export default function FileDeFrangoPage(props) {
  // Coloque aqui o código específico da página de filé de frango.
  return <CategoryPage categoryId="frango" {...props} />;
}
