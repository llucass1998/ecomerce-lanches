import CategoryPage from '../CategoryPage.jsx';

export default function PorcoesPage(props) {
  // Coloque aqui o código específico da página de porções.
  return <CategoryPage categoryId="porcoes" {...props} />;
}
