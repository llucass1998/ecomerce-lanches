import CategoryPage from '../CategoryPage.jsx';

export default function AcaiPage(props) {
  // Coloque aqui o código específico da página de açaí.
  return <CategoryPage categoryId="acai" {...props} />;
}
