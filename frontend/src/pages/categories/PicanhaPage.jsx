import CategoryPage from '../CategoryPage.jsx';

export default function PicanhaPage(props) {
  // Coloque aqui o código específico da página de picanha.
  return <CategoryPage categoryId="picanha" {...props} />;
}
